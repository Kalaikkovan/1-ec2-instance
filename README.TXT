READ ME !

Executed the below steps to push the file to GIT

git init

git add .

git remote add origin https://github.com/Kalaikkovan/1-ec2-instance.git

git commit -m "initial commit"

git config --global user.email "kalaikovan.u@gmail.com"

git config --local -l

git push origin master
	supplement UID and PWD of github.com
